# Pando

> Placeholder — full README written in the repo-split follow-up (4b).

Hookified core for building Claude Code companion bridges. See `pando/server.py`
(`create_app(config)`) and `pando/memory_provider.py` for the pluggable memory
contract. `examples/memory_stub.py` demonstrates the 4-endpoint HTTP contract;
`static/` ships a minimal demo frontend.
